#  Magic Mock

### Install 

**`npm install`**

If running  **`npm install`** fails, use  **`npm run install`**.

### Start Cmd
> **Frontend**: **`npm run frontend`**
**Backend**: **`npm run backend`**

### How to use it?

